let prevTime = 0;
let elapsed = 0;

let score;
let rowsCleared;
let isGameOver;

let player;
let board;

const game = {
  prevTime,
  elapsed,
  score,
  rowsCleared,
  isGameOver,
  player,
  board
};

module.exports = game;
